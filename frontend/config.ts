// Application configuration
export const appName = "AutoImageResizer";
export const appVersion = "1.0.0";

// Demo authentication - in production, integrate with your preferred auth provider
export const demoMode = true;
